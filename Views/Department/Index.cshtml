
@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Departman Üyeleri</h2>

<div class="container">
    <a class="btn btn-default" href="@Url.Action("AddUser","Department",new { id=1007 })" style="float:right;">Kullanıcı Ekle</a>
    <table class="table table-bordered" id="example">
        <thead>
            <tr>
                <th>İsim</th>
                <th>Yönetim</th>
            </tr>
        </thead>
        <tbody></tbody>
    </table>
</div>
<script>
    function pdfcreate() {
        alert("Pdf Oluşturuldu.")

    }


    $(document).ready(function () {
        $('#example').DataTable({
            "ajax": {
                "url": "Department/GetsDepartment/1007",
                "dataSrc": ""
            },
            "columns": [
                { "data": "name" },
                {
                  "title": "Kaldır",
                    "data": "email",
                  "searchable": false,
                  "sortable": false,
                    "render": function (data, type, row) {
                        var veri = data;
                        return '<a href="@Url.Action("DeleteUser", "Department")?email=' + veri + '">Departmandan Sil</a>';},
                },
            ]
        });
    });

</script> 